#include<iostream>
using namespace std;
int main()
{
    int pa,add,e;
    float pa_in;
    cin>>pa>>add>>e>>pa_in;
    cout<<pa_in<<endl;
    if((pa>=21) && (pa<=60) && (add>=18) && (e>=1) && (e<=4) && (pa_in<=300000))
    {
        cout<<"Yes, You are eligible for SBI credit cards.";
    }
    else
    {
        cout<<"No, You are not eligible for SBI credit cards.";
    }
}